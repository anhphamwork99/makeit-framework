{
    "buildCommand": "rm -rf docs/content && cp -r wiki docs/content && cd docs && npm install --legacy-peer-deps && npx vite build",
    "outputDirectory": "docs/dist",
    "framework": null,
    "installCommand": "cd docs && npm install --legacy-peer-deps",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}