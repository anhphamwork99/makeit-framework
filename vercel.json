{
    "buildCommand": "rm -rf docs/content && cp -r wiki docs/content && cd docs && pnpm install && pnpm build",
    "outputDirectory": "docs/dist",
    "framework": "vite",
    "installCommand": "corepack enable && cd docs && pnpm install",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}